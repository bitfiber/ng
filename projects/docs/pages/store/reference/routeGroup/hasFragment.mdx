import {Badge} from '../../../../';

# hasFragment <Badge type={'method'}>Method</Badge>

Determines whether the current route includes a fragment

## API

```ts copy
hasFragment(): boolean;
```

## Example

```ts copy
import {routeGroup} from '@bitfiber/ng/rx';

// Creates a route group
const route = routeGroup<{id: number; type: string}>({
  initialParams: {id: 0, type: 'all'},
});

if (route.hasFragment()) {
  // Some logic
}
```
